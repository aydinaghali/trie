//#include "Trie.h"

/*template <class T>
Node<T>::Node(){
	isTerminal = true;	
}

template <class T>
Node<T>::~Node(){

}

template <class T>
void Node<T>::addItem(T key, Node<T> *child){
	keys.push_back(key);
	children.push_back(child);
}

template <class T>
Node<T> *Node<T>::getItem(T key){
	int keyPosition;	
	for(int i=0; i<keys.size(); ++i){
		if(keys[i] == key){
			keyPosition = i;
		}else{
			return NULL;
		}
	}

	return children[keyPosition];
}

template class Node<int>;
template class Node<char>;
*/
